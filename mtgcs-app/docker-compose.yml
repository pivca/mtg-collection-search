services:
  postgres:
    image: postgres:17
    container_name: mtgcs-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: mtgcs
      POSTGRES_USER: mtgcs
      POSTGRES_PASSWORD: mtgcs
    volumes:
      - mtgcs-data:/var/lib/postgresql/data

volumes:
  mtgcs-data:
